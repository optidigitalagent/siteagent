# Next Action

Checkpoint: `BELLA_CALIBRATION_SCOPE_RESOLUTION_RECOVERABLE`.

Orange Beauty Studio is accepted as a no-publish Level B calibration: the final source has
three semantic sections, two authorised Cloudinary business-media treatments, fresh
desktop/tablet/mobile screenshots, a clean technical gate, a 100-point scope-aware
commercial gate, an independent 91-point review, and a passing local acceptance audit.

Bella Dent Clinic has completed reusable research, authorised-media preparation, reference
selection (excluding its own legacy public site), design brief, implementation package and
Studio input. The interrupted concept task must be resumed, but only after applying the new
more-restrictive strategist scope override: the generated research recommends `micro_site`;
the former generic media counter incorrectly wrote `full_site`. Re-run the existing Bella
calibration from `runs/bella-dent-clinic-calibration` without deleting artifacts or rerunning
completed research/media/design stages. It must produce one concept, no more than three
semantic sections and two image treatments, then receive fresh screenshot-led critic/fixer,
commercial, acceptance, and no-publish evidence.

Do not run `go`, Telegram, Cloudflare, or any production publish. Keep
`CREATIVE_STUDIO_HUMAN_CALIBRATION_REQUIRED=true`.
