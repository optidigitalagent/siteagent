# Next Action

Configure local Cloudinary and OpenAI credentials without recording them, then run
`python -m site_agent.reference_import`. Prepare authorised real-user media manifests
for Orange Beauty Studio and Bella Dent Clinic. If either asset set cannot prove
`user_authorized=true` and `allowed_for_public_site=true`, stop at media-input and
report the exact missing assets. Keep `CREATIVE_STUDIO_HUMAN_CALIBRATION_REQUIRED=true`;
do not run `go`, Telegram, Cloudflare, publishing, or delivery.
